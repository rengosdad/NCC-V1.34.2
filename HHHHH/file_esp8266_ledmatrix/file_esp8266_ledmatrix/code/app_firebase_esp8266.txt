

#include <ESP8266WiFi.h>
#include <FirebaseESP8266.h>

//1. Change the following info
#define WIFI_SSID "0374255776"
#define WIFI_PASSWORD "79797979"
#define FIREBASE_HOST "espfirebase-293a9-default-rtdb.firebaseio.com"
#define FIREBASE_AUTH "rv6fo8qWS7MWiScn5TwmfNyxw53kPtWIApGANojM"

//2. Define FirebaseESP8266 data object for data sending and receiving
FirebaseData fbdo;
String dulieu;
int i =0;
void setup()
{

  Serial.begin(9600);

  WiFi.begin(WIFI_SSID, WIFI_PASSWORD);
  Serial.print("Connecting to Wi-Fi");
  while (WiFi.status() != WL_CONNECTED)
  {
    Serial.print(".");
    delay(300);
  }
  Serial.println();
  Serial.print("Connected with IP: ");
  Serial.println(WiFi.localIP());
  Serial.println();
  Firebase.begin(FIREBASE_HOST, FIREBASE_AUTH);
  Firebase.reconnectWiFi(true);

 
 




}

void loop()
{
  if(i == 0){
    if(Firebase.getString(fbdo, "/vidu/data"))
    {
      dulieu = fbdo.stringData();
      dulieu.remove(0,2);
      dulieu.remove(dulieu.length()-2,2);
      Serial.print(dulieu);
      i = 1; 
    }
  }
  if(i == 1){
    Firebase.deleteNode(fbdo, "/vidu/data");
    i = 0;
  }
  
  
  
}
 